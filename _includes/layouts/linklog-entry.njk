---
layout: layouts/base.njk
title: Linklog
permalink: "/linklog/{{ fileSlug }}/"
---

<article
    class="linklog-entry dense h-entry">
    <!-- Top bar: favicon + title + date -->
    <header>
        <h3 class="title">
            <a class="u-bookmark-of" href="{{ externalUrl }}" target="_blank" rel="noopener">
                <span
                    class="favicon"
                    style="background-image: url('https://www.google.com/s2/favicons?domain={{ externalUrl | url }}');"
                    aria-hidden="true"></span>
                {{- title -}}
            </a>
        </h3>
        <div class="date-added">Added on <time class="dt-published" datetime="{{ date }}">
                {{ date | dateWithSuffix }}
            </time>
        </div>
        <!-- Optional "via" source -->
        {% if via %}
            <div class="via">
                        Found via
                        <a href="{{ via }}" target="_blank" rel="noopener">
                    <span
                    class="favicon"
                    style="background-image: url('https://www.google.com/s2/favicons?domain={{ via | url }}');"
                    aria-hidden="true"></span>{{ via }}</a>
            </div>
        {% endif %}
    </header>
    <!-- Comment / description / blockquote -->
    <div class="e-content">
        {{ content | safe }}
    </div>
</article>